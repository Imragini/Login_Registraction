let Home = () => {
  return (
    <>
      <h1>Home Page</h1>
      <hr />
      <p> Home Page
        Fontsource can be configured to load specific subsets, weights and
        styles. Material UI's default typography configuration relies only on
        the 300, 400, 500, and 700 font weights.
        Fontsource can be configured to load specific subsets, weights and
        styles. Material UI's default typography configuration relies only on
        the 300, 400, 500, and 700 font weights.
        Fontsource can be configured to load specific subsets, weights and
        styles. Material UI's default typography configuration relies only on
        the 300, 400, 500, and 700 font weights.
        Fontsource can be configured to load specific subsets, weights and
        styles. Material UI's default typography configuration relies only on
        the 300, 400, 500, and 700 font weights.
        Fontsource can be configured to load specific subsets, weights and
        styles. Material UI's default typography configuration relies only on
        the 300, 400, 500, and 700 font weights.
      </p>
    </>
  );
};

export default Home;
